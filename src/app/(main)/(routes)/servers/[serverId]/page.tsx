import { FC } from "react";
interface ServerIdPageProps { };

const ServerIdPage: FC<ServerIdPageProps> = (props) => {
    return (
        <div>
            ServerIdPage
        </div>
    );
}
export default ServerIdPage;
